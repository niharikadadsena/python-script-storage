# Local Laboratory Workspaces
This workspace tracks defensive Python automation tools, encryption algorithms, and local system security scripts compiled throughout my self-directed training partitions.

---

## Module Tool: Local Storage Archive Utility (`archive_utility.py`)

### Core Algorithmic Logic
* Inverse Modular Transposition: Implements a custom reverse-direction substitution cipher. By processing character streams utilizing a negative translation offset wrapped via modular arithmetic (`% 26`), the script ensures a highly unique cryptographic footprint that shifts away from standard textbook additions.
* Structural Case Maintenance: Dynamically calculates uppercase vs. lowercase ASCII starting boundaries (`ord('A')` / `ord('a')`) to execute character shifts smoothly without corrupting alphanumeric data bounds, spaces, or system formatting syntax.

### OS Partition & Telemetry Logging
* Dynamic I/O Buffering: Uses native Python file handlers (`with open()`) to write raw inputs to local sectors, verify data stream persistence, and shift the payload into an insulated target data file (`secured_data.dat`).
* Automated Audit Trails: Interacts directly with system runtime data (`import datetime`) to pipe execution clocks, key magnitudes, and total processed character length into an unalterable transaction log file (`security_status.log`) for administrative review. 
